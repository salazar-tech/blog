#!/bin/bash
docker run --rm -it \
  -p 4000:4000 \
  -p 35729:35729 \
  -v "$(pwd):/srv/jekyll" \
  -e JEKYLL_ENV=development \
  -w /srv/jekyll \
  jekyll/jekyll:latest \
  jekyll serve --incremental --livereload
